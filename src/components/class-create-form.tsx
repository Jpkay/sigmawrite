"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { createClass } from "@/lib/actions/teacher";
export function ClassCreateForm({language="fr"}:{language?:"fr"|"en"}) { const en=language==="en";const router=useRouter(); const [name,setName]=useState(""); const [grade,setGrade]=useState(7); const [year,setYear]=useState("2026–2027"); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{const result=await createClass({name,grade,academicYear:year});router.push(`/teacher/classes/${result.classId}`);}catch{setError(en?"The class could not be created.":"La classe n’a pas pu être créée.");setBusy(false);}} return <Card className="mb-6"><CardContent className="pt-6"><form onSubmit={submit} className="grid gap-3 sm:grid-cols-4"><label className="text-sm sm:col-span-2">{en?"Name":"Nom"}<input value={name} onChange={(e)=>setName(e.target.value)} className="mt-1 h-9 w-full rounded-md border border-input bg-background px-3" placeholder="5e A" required /></label><label className="text-sm">{en?"Grade":"Niveau"}<input type="number" min={5} max={12} value={grade} onChange={(e)=>setGrade(Number(e.target.value))} className="mt-1 h-9 w-full rounded-md border border-input bg-background px-3" /></label><label className="text-sm">{en?"Year":"Année"}<input value={year} onChange={(e)=>setYear(e.target.value)} className="mt-1 h-9 w-full rounded-md border border-input bg-background px-3" /></label><div className="sm:col-span-4"><Button disabled={busy}>{busy?(en?"Creating…":"Création…"):(en?"Create class":"Créer la classe")}</Button>{error&&<span className="ml-3 text-sm text-destructive">{error}</span>}</div></form></CardContent></Card>; }
