"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{setPreferredLanguage}from"@/lib/actions/preferences";export function LanguageToggle({language}:{language:"fr"|"en"}){const router=useRouter();const[busy,setBusy]=useState(false);async function change(next:"fr"|"en"){setBusy(true);try{await setPreferredLanguage({language:next});router.refresh();}finally{setBusy(false);}}return <div className="mb-4 flex justify-end gap-1" aria-label="Language"><button disabled={busy||language==="fr"} onClick={()=>change("fr")} className="min-h-9 rounded-md border border-border px-3 text-xs disabled:bg-primary/15">FR</button><button disabled={busy||language==="en"} onClick={()=>change("en")} className="min-h-9 rounded-md border border-border px-3 text-xs disabled:bg-primary/15">EN</button></div>}
