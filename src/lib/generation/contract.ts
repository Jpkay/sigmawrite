import{createHash}from"node:crypto";import{z}from"zod";
const id=z.string().min(1),range=z.object({min:z.number().min(0).max(1),max:z.number().min(0).max(1)}).refine(v=>v.max>=v.min);
export const generationContractSchema=z.object({schemaVersion:z.literal("1.0.0"),contractId:id,createdAt:z.string().datetime(),studentContext:z.object({learnerMode:z.enum(["french_first_language","french_second_language","heritage","bilingual","allophone","immersion"]),currentLevel:id,goal:id,accommodations:z.array(z.string())}),releases:z.object({taxonomyReleaseId:id,taxonomyChecksum:id,lexicalReleaseId:id,lexicalChecksum:id,ontologyVersion:id}),topic:z.object({label:id,interestId:z.string().optional(),riskClass:z.enum(["low","medium","high"]),sourceRequirement:z.enum(["none","stable_authoritative","current_primary_sources"]),recentContentExclusions:z.array(z.string())}),content:z.object({textType:id,lengthWords:z.object({min:z.number().int().positive(),max:z.number().int().positive()}),enduranceDemand:z.enum(["short","standard","extended"])}),difficulty:z.object({predictedSuccess:range,policy:z.enum(["normal","fluency","diagnostic_stretch","conservative"]),knownVocabularyCoverageMin:z.number().min(0).max(1),maximumUnfamiliarWords:z.number().int().nonnegative()}),vocabulary:z.object({knownIds:z.array(id),requiredTargetIds:z.array(id),support:z.record(z.string(),z.enum(["gloss","context","morphology","none"]))}),constructions:z.object({allowedIds:z.array(id),targetIds:z.array(id),excludedIds:z.array(id)}),conjugation:z.object({allowedTenses:z.array(id),targetTenses:z.array(id),stretchTenses:z.array(id),excludedTenses:z.array(id),temporalConnectors:z.array(id),maximumTenseSwitchDensity:z.number().min(0).max(1),assessmentFocus:z.array(z.enum(["form","meaning","chronology","discourse_effect"]))}),competencies:z.object({targetIds:z.array(id).min(1),prerequisiteIds:z.array(id),remediationIds:z.array(id)}),concepts:z.object({requiredIds:z.array(id),explainIds:z.array(id),factualGrounding:z.array(z.object({conceptId:id,requirement:id}))}),courseContext:z.object({courseVersionId:z.string().nullable(),moduleVersionId:z.string().nullable(),lessonVersionId:z.string().nullable(),completionTargets:z.array(id)}),retrieval:z.array(z.object({targetType:id,targetId:id,mode:z.enum(["direct","indirect"]),presentation:z.enum(["recognition","production","contextual_reappearance"]),evidenceKey:id})),questions:z.object({allowedTypes:z.array(id),counts:z.record(z.string(),z.number().int().nonnegative()),difficulty:range,targetCompetencyIds:z.array(id)}),safety:z.object({ageBand:id,prohibitedContent:z.array(id),factualityMode:id,requiresCitations:z.boolean(),promptInjectionPolicy:z.literal("fail_closed")}),retry:z.object({maximumAttempts:z.number().int().min(1).max(10),repairableFailures:z.array(id),escalateOn:z.array(id),humanReviewRequired:z.boolean()}),adminExplanation:z.object({targetDecisionId:id,reasons:z.array(id),constraintSources:z.record(z.string(),z.string())})}).superRefine((v,ctx)=>{if(v.topic.riskClass==="high"&&(v.topic.sourceRequirement!=="current_primary_sources"||!v.safety.requiresCitations))ctx.addIssue({code:"custom",message:"High-risk topics require current primary sources and citations",path:["topic"]});if(v.difficulty.policy==="normal"&&(v.difficulty.predictedSuccess.min<.75||v.difficulty.predictedSuccess.max>.85))ctx.addIssue({code:"custom",message:"Normal difficulty must remain in the 75–85% zone",path:["difficulty"]});if(v.content.lengthWords.max<v.content.lengthWords.min)ctx.addIssue({code:"custom",message:"Invalid word range",path:["content","lengthWords"]});});
export type GenerationContract=z.infer<typeof generationContractSchema>;export type ContractInput=Omit<GenerationContract,"schemaVersion"|"contractId"|"createdAt">;
function stable(value:unknown):string{if(Array.isArray(value))return`[${value.map(stable).join(",")}]`;if(value&&typeof value==="object")return`{${Object.entries(value).sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${JSON.stringify(k)}:${stable(v)}`).join(",")}}`;return JSON.stringify(value);}
export function buildGenerationContract(input:ContractInput,createdAt:string):GenerationContract{const content={schemaVersion:"1.0.0"as const,createdAt,...input},contractId=`gc_${createHash("sha256").update(stable(content)).digest("hex")}`;return generationContractSchema.parse({...content,contractId});}
export function generationContractChecksum(contract:GenerationContract){return`sha256:${createHash("sha256").update(stable(contract)).digest("hex")}`;}
