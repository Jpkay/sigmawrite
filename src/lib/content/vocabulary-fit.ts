export function rankByInterestAndVocabulary<T extends{id:string;primaryInterest:string|null}>(items:T[],interestOrder:string[],targets:Map<string,string[]>,known:Set<string>):T[]{const interestRank=new Map(interestOrder.map((key,index)=>[key,index]));return[...items].sort((a,b)=>score(a)-score(b));function score(item:T){const interest=interestRank.get(item.primaryInterest??"")??999;const words=targets.get(item.id)??[];const unknown=words.length?words.filter(id=>!known.has(id)).length/words.length:0;return interest*100+unknown*10;}}
