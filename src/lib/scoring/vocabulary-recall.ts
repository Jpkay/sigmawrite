import type{RetrievalResult}from"./retrieval";function normalize(value:string){return value.normalize("NFC").trim().toLocaleLowerCase("fr").replace(/[’']/g,"'");}function distance(a:string,b:string){const row=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){let prior=row[0];row[0]=i;for(let j=1;j<=b.length;j++){const old=row[j];row[j]=Math.min(row[j]+1,row[j-1]+1,prior+(a[i-1]===b[j-1]?0:1));prior=old;}}return row[b.length];}export function gradeTypedVocabularyRecall(answer:string,expected:string):RetrievalResult{const a=normalize(answer),e=normalize(expected);if(a===e)return"good";return a&&distance(a,e)<=1?"hard":"forgot";}
