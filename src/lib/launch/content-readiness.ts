export type LaunchContentCounts={activeReviewers:number;publishedPassages:number;publishedReviewVersions:number;submittedAssignments:number;lockedBenchmarks:number;publishedDiagnosticBanks:number;publishedTaxonomyReleases:number};export function assessLaunchContent(counts:LaunchContentCounts){const issues:string[]=[];if(counts.activeReviewers<3)issues.push(`At least 3 active real reviewers are required; found ${counts.activeReviewers}.`);if(counts.publishedPassages<60)issues.push(`At least 60 human-approved passages are required; found ${counts.publishedPassages}.`);if(counts.publishedReviewVersions<60)issues.push(`At least 60 editorial review versions must be published; found ${counts.publishedReviewVersions}.`);if(counts.submittedAssignments<180)issues.push(`At least 180 independent review assignments are required for 60 passages × 3 reviewers; found ${counts.submittedAssignments}.`);if(counts.lockedBenchmarks!==6)issues.push(`Exactly 6 benchmarks must be locked; found ${counts.lockedBenchmarks}.`);if(counts.publishedDiagnosticBanks<1)issues.push("A validated diagnostic item-bank release must be published.");if(counts.publishedTaxonomyReleases<1)issues.push("A taxonomy release must be published.");return{ready:issues.length===0,issues,counts};}
