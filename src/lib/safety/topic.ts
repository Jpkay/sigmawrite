export type SanitizedTopic={allowed:true;value:string;reason?:never}|{allowed:false;value:"";reason:"empty"|"too_long"|"control_characters"|"prompt_injection"};
const INJECTION_PATTERNS=[/\b(?:ignore|forget|disregard|override|reveal|repeat)\b.{0,40}\b(?:instruction|prompt|system|developer|message|policy)\b/iu,/\b(?:oublie|ignore|contourne|remplace|révèle|affiche|répète)\b.{0,50}\b(?:instruction|consigne|invite|prompt|système|développeur|politique)\b/iu,/(?:<\/?(?:system|developer|assistant)>|\[\s*(?:system|developer)\s*\]|```\s*(?:system|prompt))/iu,/\b(?:jailbreak|dan mode|system prompt|developer message)\b/iu];
export function sanitizeStudentTopic(input:string):SanitizedTopic{const normalized=input.normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g,"").replace(/\s+/g," ").trim();if(!normalized)return{allowed:false,value:"",reason:"empty"};if(normalized.length>160)return{allowed:false,value:"",reason:"too_long"};if(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F]/u.test(normalized))return{allowed:false,value:"",reason:"control_characters"};if(INJECTION_PATTERNS.some(p=>p.test(normalized)))return{allowed:false,value:"",reason:"prompt_injection"};return{allowed:true,value:normalized};}

