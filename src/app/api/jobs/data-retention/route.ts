import{applyEventRetention,authorizeJob,fulfillDeletionRequests,withJobRun}from"@/lib/jobs";export async function GET(request:Request){if(!authorizeJob(request))return Response.json({error:"unauthorized"},{status:401});const result=await withJobRun("data_retention",async db=>{const[deleted,events]=await Promise.all([fulfillDeletionRequests(db),applyEventRetention(db)]);return{result:{deleted,events},processed:deleted+events};});return Response.json({ok:true,...result});}
