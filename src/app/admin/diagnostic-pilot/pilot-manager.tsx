"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { enrollDiagnosticPilotStudent, revokeDiagnosticPilotEnrollment, setDiagnosticPilotEnabled } from "@/lib/actions/diagnostic-pilot";

type Student = { id: string; name: string; grade: number };
type Enrollment = { id: string; studentId: string; studentName: string; grade: number; active: boolean; expiresAt: string; note: string | null; cohortKind: "internal_test" | "feedback_participant"; agreementSource: "student" | "guardian" | null };

export function DiagnosticPilotManager({ enabled, students, enrollments }: { enabled: boolean; students: Student[]; enrollments: Enrollment[] }) {
  const router = useRouter();
  const [studentId, setStudentId] = useState(students[0]?.id ?? "");
  const [days, setDays] = useState(7);
  const [note, setNote] = useState("");
  const [cohortKind, setCohortKind] = useState<"internal_test" | "feedback_participant">("feedback_participant");
  const [agreementSource, setAgreementSource] = useState<"student" | "guardian">("student");
  const [agreementConfirmed, setAgreementConfirmed] = useState(false);
  const [busy, setBusy] = useState("");
  const [message, setMessage] = useState("");
  async function act(key: string, work: () => Promise<unknown>) {
    setBusy(key); setMessage("");
    try { await work(); router.refresh(); }
    catch (error) { setMessage(error instanceof Error ? error.message : "Action impossible."); }
    finally { setBusy(""); }
  }
  const active = enrollments.filter((enrollment) => enrollment.active);
  const agreementReady = cohortKind === "internal_test" || agreementConfirmed;
  return <div className="space-y-7"><Card><CardContent className="flex flex-wrap items-center justify-between gap-4 pt-6"><div><div className="flex items-center gap-2"><h2 className="font-semibold">Interrupteur global</h2><Badge variant={enabled ? "success" : "secondary"}>{enabled ? "Actif" : "Désactivé"}</Badge></div><p className="mt-1 max-w-2xl text-sm text-muted-foreground">La désactivation bloque immédiatement le démarrage et la prochaine question de tous les essais.</p></div><Button variant={enabled ? "destructive" : "default"} disabled={Boolean(busy)} onClick={() => void act("toggle", () => setDiagnosticPilotEnabled({ enabled: !enabled }))}>{enabled ? "Arrêter tous les essais" : "Autoriser les essais"}</Button></CardContent></Card><Card><CardContent className="pt-6"><h2 className="font-semibold">Inscrire un participant</h2><p className="mt-1 text-sm text-muted-foreground">Un participant de feedback doit avoir donné son accord volontaire. Un compte ayant déjà terminé un diagnostic publié est refusé automatiquement.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><select aria-label="Élève" value={studentId} onChange={(event) => setStudentId(event.target.value)} className="h-10 rounded-md border border-input bg-background px-3 text-sm">{students.map((student) => <option key={student.id} value={student.id}>{student.name} · classe {student.grade}</option>)}</select><select aria-label="Type de participant" value={cohortKind} onChange={(event) => { setCohortKind(event.target.value as typeof cohortKind); setAgreementConfirmed(false); }} className="h-10 rounded-md border border-input bg-background px-3 text-sm"><option value="feedback_participant">Participant feedback</option><option value="internal_test">Compte de test interne</option></select><input aria-label="Durée en jours" type="number" min={1} max={30} value={days} onChange={(event) => setDays(Number(event.target.value))} className="h-10 rounded-md border border-input bg-background px-3 text-sm" /><input aria-label="Note" value={note} onChange={(event) => setNote(event.target.value)} placeholder="But de l’essai" className="h-10 rounded-md border border-input bg-background px-3 text-sm" /></div>{cohortKind === "feedback_participant" && <div className="mt-4 space-y-3 border-y border-border py-4"><label className="block max-w-sm text-sm">Accord donné par<select className="mt-1 h-10 w-full rounded-md border border-input bg-background px-3" value={agreementSource} onChange={(event) => setAgreementSource(event.target.value as typeof agreementSource)}><option value="student">L’élève (15 ans ou plus)</option><option value="guardian">Le responsable de l’élève</option></select></label><label className="flex items-start gap-3 text-sm"><input type="checkbox" className="mt-0.5 size-4 accent-primary" checked={agreementConfirmed} onChange={(event) => setAgreementConfirmed(event.target.checked)} /><span>J’atteste que cet accord volontaire a été obtenu pour la version « feedback-pilot-v1 ». Il est distinct de l’autorisation scolaire d’utiliser Plume.</span></label></div>}<Button className="mt-4" disabled={Boolean(busy) || !studentId || !agreementReady} onClick={() => void act("enroll", () => enrollDiagnosticPilotStudent({ studentId, durationDays: days, note, cohortKind, feedbackAgreementSource: cohortKind === "feedback_participant" ? agreementSource : undefined, agreementConfirmed: cohortKind === "feedback_participant" ? agreementConfirmed : undefined }))}>Inscrire</Button></CardContent></Card>{message && <p role="status" className="text-sm text-destructive">{message}</p>}<section><div className="mb-3 flex items-center justify-between"><h2 className="font-semibold">Accès actifs</h2><span className="text-sm text-muted-foreground">{active.length}</span></div>{active.length ? <div className="space-y-2">{active.map((enrollment) => <Card key={enrollment.id}><CardContent className="flex flex-wrap items-center justify-between gap-4 pt-6"><div><div className="flex flex-wrap items-center gap-2"><p className="font-medium">{enrollment.studentName} · classe {enrollment.grade}</p><Badge variant={enrollment.cohortKind === "feedback_participant" ? "success" : "secondary"}>{enrollment.cohortKind === "feedback_participant" ? "Feedback" : "Test interne"}</Badge></div><p className="mt-1 text-xs text-muted-foreground">Expire le {new Date(enrollment.expiresAt).toLocaleString("fr-FR")}{enrollment.agreementSource ? ` · accord : ${enrollment.agreementSource === "student" ? "élève" : "responsable"}` : ""}{enrollment.note ? ` · ${enrollment.note}` : ""}</p></div><Button variant="outline" disabled={Boolean(busy)} onClick={() => void act(enrollment.id, () => revokeDiagnosticPilotEnrollment({ enrollmentId: enrollment.id }))}>Révoquer</Button></CardContent></Card>)}</div> : <p className="text-sm text-muted-foreground">Aucun participant inscrit.</p>}</section></div>;
}
