import{PageHeader}from"@/components/page";import{getViewableStudents}from"@/lib/db/dashboard";import{ChildPasswordForm}from"./child-password-form";export default async function Page(){const children=await getViewableStudents();return <><PageHeader title="Paramètres" description="Sécurité du compte et des enfants liés."/><ChildPasswordForm students={children.map(child=>({id:child.id,name:child.name}))}/></>}
