begin;create extension if not exists pgtap with schema extensions;select plan(12);select has_column('public','competency_items','quiz_purposes','Item purpose is explicit');select has_column('public','competency_items','prompt_family','Prompt variety is tracked');select has_table('public','quiz_policies','Fatigue policy is versioned');select has_table('public','quiz_sessions','Quiz lifecycle exists');select has_column('public','quiz_sessions','current_position','Quiz can resume');select has_column('public','quiz_sessions','assembly_snapshot','Assembly is auditable');select has_table('public','quiz_session_items','Item-node links are frozen');select has_column('public','quiz_session_items','misconception_ids','Misconceptions are linked');select has_column('public','quiz_session_items','modality','Modality is linked');select has_table('public','quiz_responses','Responses are evidence');select col_is_unique('public','quiz_responses',array['student_id','evidence_occurrence_key'],'Evidence cannot double count');select has_table('public','quiz_remediation_triggers','Persistent gaps trigger remediation');select * from finish();rollback;
