begin;create extension if not exists pgtap with schema extensions;select plan(10);select has_table('public','generation_contract_schema_versions','Contract schema is versioned');select has_table('public','generation_contracts','Contracts persist');select has_column('public','generation_contracts','target_decision_id','Resolver decision is pinned');select has_column('public','generation_contracts','taxonomy_release_id','Taxonomy release is exact');select has_column('public','generation_contracts','lexical_release_id','Lexical release is exact');select has_column('public','generation_contracts','contract_payload','All constraints are structured');select has_column('public','generation_contracts','payload_checksum','Payload is checksummed');select has_column('public','generation_contracts','admin_explanation','Admins receive explanations');select has_trigger('public','generation_contracts','immutable_generation_contract','Contracts are immutable');select col_is_unique('public','generation_contracts',array['schema_version_id','payload_checksum'],'Identical versioned contracts deduplicate');select * from finish();rollback;
