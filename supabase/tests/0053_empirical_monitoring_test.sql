begin;
set local role postgres;
set local search_path = public, extensions;create extension if not exists pgtap with schema extensions;select plan(12);select has_table('public','empirical_metric_definitions','Metric thresholds are documented');select has_column('public','empirical_metric_definitions','minimum_sample','Minimum sample is explicit');select has_table('public','empirical_metric_snapshots','Versioned metric snapshots persist');select has_column('public','empirical_metric_snapshots','learner_mode','Drift can group by learner mode');select has_column('public','empirical_metric_snapshots','content_type','Drift can group by content type');select has_column('public','empirical_metric_snapshots','status','Insufficient evidence remains inactive');select has_table('public','empirical_adjustment_proposals','Recalibration is proposed, not silently applied');select has_column('public','empirical_adjustment_proposals','adjustment','Adjustment is bounded');select has_column('public','empirical_adjustment_proposals','reviewed_by','Human approval is attributable');select has_table('public','empirical_review_events','Anomalies enter review queue');select has_column('public','empirical_review_events','explanation','Review events are explainable');select has_column('public','empirical_review_events','status','Exceptions have lifecycle');select * from finish();rollback;
