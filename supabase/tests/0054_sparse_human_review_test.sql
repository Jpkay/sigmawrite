begin;create extension if not exists pgtap with schema extensions;select plan(12);select has_table('public','sparse_review_policies','Sampling policy is configurable');select has_column('public','sparse_review_policies','low_risk_sample_percent','Low risk is sampled');select has_column('public','sparse_review_policies','always_review_high_risk','High risk escalates');select has_table('public','pipeline_audit_campaigns','Pipeline versions can be audited');select has_table('public','sparse_review_cases','Exception cases exist');select has_column('public','sparse_review_cases','primary_reason','Queue reason is explicit');select has_column('public','sparse_review_cases','selection_explanation','Selection is explained');select has_table('public','sparse_review_evidence','QA and observed evidence are comparable');select has_column('public','sparse_review_evidence','observed_evidence','Observed evidence is retained');select has_table('public','sparse_review_actions','Overrides and retirement are audited');select has_column('public','sparse_review_actions','qa_calibration_feedback','Review feedback calibrates QA');select has_trigger('public','sparse_review_actions','immutable_sparse_review_action','Overrides are immutable');select * from finish();rollback;
