begin;
set local role postgres;
set local search_path = public, extensions;create extension if not exists pgtap with schema extensions;select plan(12);select has_table('public','automation_benchmark_suites','Benchmark versions persist');select has_column('public','automation_benchmark_suites','manifest_checksum','Suite is checksummed');select has_trigger('public','automation_benchmark_suites','immutable_benchmark_suite','Suite versions are immutable');select has_table('public','automation_benchmark_cases','Cases persist');select has_column('public','automation_benchmark_cases','expected_result','Expected failures are first-class');select has_column('public','automation_benchmark_cases','critical','Critical cases block release');select has_trigger('public','automation_benchmark_cases','immutable_benchmark_case','Cases are immutable');select has_table('public','automation_benchmark_runs','Pipeline versions are compared');select has_table('public','automation_benchmark_results','Per-case outcomes persist');select has_column('public','automation_benchmark_results','matched_expected','Regression is explicit');select has_table('public','automation_release_reports','Release reports persist');select has_column('public','automation_release_reports','decision','Rollout can be blocked');select * from finish();rollback;
