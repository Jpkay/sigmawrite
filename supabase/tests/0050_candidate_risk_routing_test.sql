begin;create extension if not exists pgtap with schema extensions;select plan(12);select has_table('public','content_risk_taxonomy','Risk taxonomy exists');select has_column('public','content_risk_taxonomy','grounding_policy','Grounding policy is explicit');select has_column('public','content_risk_taxonomy','review_policy','Review routing is explicit');select has_table('public','candidate_factual_claims','Claims are catalogued');select has_column('public','candidate_factual_claims','claim_kind','Numerical/time-sensitive claims differ');select has_column('public','candidate_factual_claims','source_evidence','Sources are traceable');select has_column('public','candidate_factual_claims','current_source','Currency is recorded');select has_table('public','candidate_risk_results','Risk decisions persist');select has_column('public','candidate_risk_results','content_mode','Fiction and fact differ');select has_column('public','candidate_risk_results','copyright_analysis','Copyright policy is evidenced');select has_column('public','candidate_risk_results','reasons','Failure reasons are structured');select col_is_unique('public','candidate_risk_results',array['candidate_id','policy_version'],'One decision per candidate/policy');select * from finish();rollback;
