begin;create extension if not exists pgtap with schema extensions;select plan(4);select has_table('public','content_publication_claims','Publication has durable claim');select col_is_pk('public','content_publication_claims','candidate_id','One publication per candidate');select function_privs_are('public','claim_content_publication',array['uuid'],'authenticated',array[]::text[],'Clients cannot claim publication');select function_privs_are('public','finish_content_publication',array['uuid','jsonb'],'authenticated',array[]::text[],'Clients cannot forge publication');select * from finish();rollback;
