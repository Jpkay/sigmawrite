begin;create extension if not exists pgtap with schema extensions;select plan(4);select has_function('public','fail_reading_completion',array['uuid','text'],'Reading failure transition exists');select has_function('public','fail_content_publication',array['uuid','text'],'Publication failure transition exists');select function_privs_are('public','fail_reading_completion',array['uuid','text'],'authenticated',array[]::text[],'Clients cannot fail claims');select function_privs_are('public','fail_content_publication',array['uuid','text'],'authenticated',array[]::text[],'Clients cannot fail publication');select * from finish();rollback;
