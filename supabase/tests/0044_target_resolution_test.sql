begin;create extension if not exists pgtap with schema extensions;select plan(10);select has_table('public','target_resolver_versions','Resolver policies are versioned');select has_table('public','student_target_decisions','Decisions are auditable');select has_column('public','student_target_decisions','state_checksum','Identical state is identifiable');select has_column('public','student_target_decisions','selected_targets','Targets are frozen');select has_column('public','student_target_decisions','blocked_targets','Prerequisite blocks are explained');select has_column('public','student_target_decisions','difficulty_policy','Difficulty policy is explicit');select has_column('public','student_target_decisions','predicted_success_min','Success zone lower bound exists');select has_column('public','student_target_decisions','predicted_success_max','Success zone upper bound exists');select has_column('public','student_target_decisions','confidence','Cold-start confidence is explicit');select has_index('public','student_target_decisions','target_decision_determinism_idx','State/version decisions are unique');select * from finish();rollback;
