# Choose branch entry by challenge, not spelling

The full-graph grammar trace selected equally unvisited branches alphabetically. This put advanced participle agreement ahead of more accessible direct- and indirect-object pronoun tasks. Branch rotation now retains its existing visit priority, then compares each branch's nearest available question target to the existing starting challenge. Alphabetical order remains only the final deterministic tie-breaker. Distances are computed once per selection from the already available candidates.

This uses existing challenge ranks and graph depth; it does not create calibrated difficulty values or change prerequisites. It supplies no mastery evidence. Domain, strand, conjugation-family and form-category allocation continue to apply before branch choice. Actual prerequisite recovery still overrides the ordinary branch entry choice.

Validation: 514 tests across 148 granular files, TypeScript and scoped ESLint pass. A regression verifies that an accessible branch can precede an alphabetically earlier advanced branch, while the advanced branch is still visited when rotation calls for it. The full symbolic audit retains 10/10 depth checks and improves from 3/8 to 4/8 mixed-profile distinctions: COD versus COI now passes. Verb-specific tense boundaries, literal versus inferential reading, reading-reference gaps and recognition versus production still fail; the strict audit exits 1.

This remains an undeployed scheduling correction requiring broader validation. The full live v10 QA run continues separately on the unchanged public runtime. No content approval or educational calibration is claimed.
