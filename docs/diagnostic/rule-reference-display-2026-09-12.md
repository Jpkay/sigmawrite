# Rule reference display

Rule headings and navigation help now use shared copy recorded before the page returns, including the missing-rule branch. The complete conjugation-table help sentence is recorded alongside its styled pieces. Curriculum tags use a shared projection for the framework name, truncated visible label and full tooltip title; the rule payload records that projection alongside the original tags.

Tests compare normal and compact tag rendering with the projection, verify unpublished-rule navigation recording, reject unauthorized visits and withhold the page after a recording failure. TypeScript passes; 425 test files and 1,834 tests pass. The server-page test explicitly mocks the server-only marker, as other route tests do.

The route inventory preserves its original hashes and records these follow-up fixes separately. This batch is not deployed during the ongoing R41 diagnostic. Runtime error/version coverage, other activity pages, complete-history activation and the full learning journey remain outstanding.
