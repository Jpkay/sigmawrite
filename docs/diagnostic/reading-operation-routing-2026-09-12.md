# Reading operation screening and follow-up

Reading previously spent eight of its nine symbolic questions on information matching and locating supporting text, then revisited information matching in another genre. That route could miss both reference-resolution and inference weaknesses.

The selector now briefly screens available retrieval, reference-resolution, inference and other reading operations. An actual incorrect answer receives fresh follow-up questions before moving on; skips do not create an error signal. After screening, bounded visits retain room for confirmation. Existing per-target limits, novelty filters, total active-time budget and domain/strand balance remain in force. Scheduling categories do not share evidence or assign mastery across skills.

The corrected full-graph benchmark now passes 10/10 depth checks and 7/8 discrimination contrasts with no invariant violations. In the inference-gap profile it collects four correct information-matching answers, three incorrect local-cause inference answers, and one question each on reference and vocabulary. In the reference-gap profile the corresponding three errors concern object-pronoun reference, with inference sampled separately. Both runs stay inside the original budget.

The verb-specific tense-frontier contrast still fails. These symbolic results establish routing coverage only, not real-world calibration, approved question quality, or full graph content availability. They do not mean all reading skills are assessed in one sitting; unresolved skills remain explicit and continue during learning.

Validation: 564 granular tests and TypeScript pass. Regression tests verify operation screening, a separately known retrieval skill and confirmed gap within nine short passages, fresh question identities, and correct handling of skips. This runtime change remains separate from the frozen 197-target candidate's ongoing browser verification.
