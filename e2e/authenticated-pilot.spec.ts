import AxeBuilder from"@axe-core/playwright";import{expect,test,type Page}from"@playwright/test";test.describe("seeded pilot role journeys",()=>{test.skip(process.env.E2E_AUTHENTICATED!=="true","requires isolated seeded Supabase");async function login(page:Page,email:string,home:RegExp){await page.goto("/login");await page.getByLabel("E-mail").fill(email);await page.getByLabel("Mot de passe").fill(process.env.E2E_DEMO_PASSWORD??"Demo-2026-Strong!");await page.getByRole("button",{name:"Se connecter",exact:true}).click();await expect(page).toHaveURL(home);}test("student security, vocabulary and mobile shell are accessible",async({page})=>{await page.setViewportSize({width:360,height:780});await login(page,"demo.eleve@reading-to-learn.test",/\/student/);for(const route of ["/student/settings","/student/vocabulary"]){await page.goto(route);await expect(page.locator("body")).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);const axe=await new AxeBuilder({page}).withTags(["wcag2a","wcag2aa"]).analyze();expect(axe.violations.filter(v=>["critical","serious"].includes(v.impact??""))).toEqual([]);}});test("parent can reach linked-child security and privacy",async({page})=>{await login(page,"parent.demo@reading-to-learn.test",/\/parent/);await page.goto("/parent/settings");await expect(page.getByText("Renouveler le mot de passe d’un enfant")).toBeVisible();await page.goto("/parent/privacy");await expect(page.locator("main")).toBeVisible();});test("teacher can reach class operations",async({page})=>{await login(page,"prof.demo@reading-to-learn.test",/\/teacher/);await page.goto("/teacher/classes");await expect(page.getByRole("heading",{name:/Classes/})).toBeVisible();});});
