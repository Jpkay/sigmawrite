import"dotenv/config";import{validateLaunchEnvironment}from"../src/lib/launch-config";const issues=validateLaunchEnvironment(process.env);if(issues.length){console.error("Launch environment is not ready:");for(const issue of issues)console.error(`- ${issue.key}: ${issue.message}`);process.exit(1);}console.log("Launch environment contract satisfied. Run the human service checks in docs/launch-gates.md.");
